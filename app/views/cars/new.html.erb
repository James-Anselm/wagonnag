<% provide(:title, 'New Car') %>
<%= link_to "Back to cars", user_path(@user), class: "back-link" %>
<h1>Add a new car</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render :partial => 'form', :locals => { :submitButtonText => 'Add Car' } %>
  </div>
</div>