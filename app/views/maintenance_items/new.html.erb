<% provide(:title, 'New Maintenance Item') %>
<%= link_to "Back to car", car_path(@car), class: "back-link" %>
<h1>Add a new maintenance item</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render :partial => 'form', :locals => { :submitButtonText => 'Add Maintenance Item' } %>
  </div>
</div>